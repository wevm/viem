---
"viem": patch
---

Updated `ox` to `0.14.30`, added TIP-1061 multisig gas estimation hints (`multisigInit`, `multisigSignatureCount`), and derived multisig bootstrap gas via `multisigInit` in `prepareTransactionRequest`.
