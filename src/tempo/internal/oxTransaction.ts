// Same symbols as `ox/tempo/Transaction`, un-renamed and addressable through
// `viem/_types/*` so a consumer's declaration emit can name them. See
// `core/internal/inference/index.ts` for why these shims exist.
export type { Rpc, Transaction } from 'ox/tempo/Transaction'
