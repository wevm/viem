import { test } from 'vitest'

test.todo(
  'behavior: returns withdrawal fee (blocked: dev node lacks live `zone_*` RPC infra)',
)

test.todo(
  'behavior: accepts custom gas limit (blocked: dev node lacks live `zone_*` RPC infra)',
)
