# Core

Owns rewritten public core modules such as `Account`, `BaseError`, `Chain`,
`Client`, and `Transport`.

Only add public module implementations here after their leaf dependencies have
landed in the v3 structure.
