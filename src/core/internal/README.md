# Internal Primitives

Owns rewritten private primitives shared by v3 modules, including type helpers,
promise helpers, UID generation, LRU cache behavior, stringify helpers, and
internal errors.

Keep these modules private unless a public API review explicitly promotes them.
