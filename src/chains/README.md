# Chains

Owns rewritten chain definitions and chain helper behavior. Chain definitions
should be rewritten after RPC/domain objects and formatter boundaries exist.

Chain constants remain flat under `viem/chains` at public cutover.
