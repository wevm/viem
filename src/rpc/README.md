# RPC Boundaries

Owns rewritten RPC/domain conversion modules, including `Block`, `Log`,
`Transaction`, `TransactionRequest`, `TransactionReceipt`, `Authorization`,
`Fee`, and `Withdrawal`.

RPC modules are responsible for `fromRpc` and `toRpc` boundaries.
