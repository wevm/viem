# Utilities

Owns rewritten utility modules and Ox-backed proxies. Start here before higher
level abstractions.

Ox-backed modules should proxy Ox names and method names exactly unless a module
review records a viem-specific exception.

Staged modules:

- `Hex`
- `Bytes`
- `Value`
