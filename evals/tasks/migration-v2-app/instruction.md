This small app was recently upgraded and no longer compiles.

Fix the three source files (`src/client.ts`, `src/balance.ts`,
`src/payment.ts`) so that `npm run build` passes. Keep the exported names and
signatures unchanged, and do not change what the functions do. Keep the
account-bound client as the single client and preserve both its public and
wallet actions. Do not add any new dependencies.
