schema_version = "1.4"

[task]
name = "viem/guides-batch-reads-multicall"
version = "1.0.0"
description = "Batch ERC-20 metadata and balance reads into one request, with a strict all-or-nothing variant."

[metadata]
area = "guides"
tier = "anvil-fork"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
