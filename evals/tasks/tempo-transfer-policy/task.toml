schema_version = "1.4"

[task]
name = "viem/tempo-transfer-policy"
version = "1.0.0"
description = "Gate TIP-20 transfers with a whitelist policy on Tempo with viem."

[metadata]
area = "tempo"
tier = "tempo"

[verifier]
timeout_sec = 900.0

[agent]
timeout_sec = 1200.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
