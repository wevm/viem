schema_version = "1.4"

[task]
name = "viem/gap-impersonated-call-from"
version = "1.0.0"
description = "Dry-run a USDC transfer as an arbitrary sender with an eth_call from override."

[metadata]
area = "gap"
tier = "anvil-fork"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
