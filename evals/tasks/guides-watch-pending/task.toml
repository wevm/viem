schema_version = "1.4"

[task]
name = "viem/guides-watch-pending"
version = "1.0.0"
description = "Watch the pending transaction pool and resolve with the first observed hash."

[metadata]
area = "guides"
tier = "anvil-fork"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
