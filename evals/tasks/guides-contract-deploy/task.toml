schema_version = "1.4"

[task]
name = "viem/guides-contract-deploy"
version = "1.0.0"
description = "Deploy a contract with constructor arguments and return its address."

[metadata]
area = "guides"
tier = "anvil-fork"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
