schema_version = "1.4"

[task]
name = "viem/trap-watcher-handle"
version = "1.0.0"
description = "Collect the first 3 new block numbers, then fully stop the watcher."

[metadata]
area = "trap"
tier = "anvil-fork"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
