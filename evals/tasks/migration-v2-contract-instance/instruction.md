This project was recently upgraded and `src/token.ts` no longer compiles.

Fix `src/token.ts` so that `npm run build` passes. Keep the exported function
names and signatures unchanged, and do not change what the functions do. Do
not add any new dependencies.
