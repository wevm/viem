schema_version = "1.4"

[task]
name = "viem/core-signatures-typed-data"
version = "1.0.0"
description = "Sign EIP-712 typed data with a local account and recover its address."

[metadata]
area = "core"
tier = "offline"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 900.0

[environment]
build_timeout_sec = 1200.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
