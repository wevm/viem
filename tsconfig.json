{
  // This configuration is used for local development and type checking.
  "extends": "./tsconfig.base.json",
  "include": ["scripts", "src", "test", "vectors"],
  "exclude": ["src/_types"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~test/*": ["./test/*"],
      "viem": ["src/index.ts"]
    }
  }
}
