# token.revokeRoles

Revokes roles for a TIP-20 token.

## Usage

:::code-group

```ts twoslash [Decorator]
import { client } from './viem.config'

const hash = await client.token.revokeRoles({
  from: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
  roles: ['issuer'],
  token: '0x20c0000000000000000000000000000000000001',
})
// @log: '0x…'
```

```ts twoslash [Standalone]
import { Actions } from 'viem/tempo'
import { client } from './viem.config'

const hash = await Actions.token.revokeRoles(client, {
  from: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
  roles: ['issuer'],
  token: '0x20c0000000000000000000000000000000000001',
})
// @log: '0x…'
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
import { Account } from 'viem'
import { Client } from 'viem/tempo'

export const client = Client.create({
  account: Account.fromPrivateKey('0x…'),
})
```

:::

## Parameters

### feeToken

- **Type:** `TokenId | Address`
- **Default:** `client.feeToken`

TIP-20 token to pay transaction fees with.

### from

- **Type:** `Address`

Address to revoke the role from.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const hash = await Actions.token.revokeRoles(client, {
  from: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266', // [!code focus]
  roles: ['issuer'],
  token: '0x20c0000000000000000000000000000000000001',
})
```

### nonceKey

- **Type:** `bigint | 'random'`

Nonce key to send the transaction under. Pass `'random'` to use a random key.

### roles

- **Type:** `readonly TokenRole[]`

Roles to revoke. Valid roles: `'defaultAdmin'`, `'pause'`, `'unpause'`, `'issuer'`, `'burnBlocked'`.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const hash = await Actions.token.revokeRoles(client, {
  from: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
  roles: ['issuer', 'pause'], // [!code focus]
  token: '0x20c0000000000000000000000000000000000001',
})
```

### token

- **Type:** `TokenId | Address`

Address or ID of the TIP-20 token.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const hash = await Actions.token.revokeRoles(client, {
  from: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
  roles: ['issuer'],
  token: 1n, // [!code focus]
})
```

## Return Value

`Hex`

The transaction hash.

## Errors

| Error | Description |
| --- | --- |
| `RpcError.ExecutionError` | The transaction failed to execute. |
