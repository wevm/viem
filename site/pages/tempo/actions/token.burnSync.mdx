# token.burnSync

Burns TIP-20 tokens from the caller's balance, and waits for the transaction to be confirmed.

## Usage

:::code-group

```ts twoslash [Decorator]
import { client } from './viem.config'

const { from, amount, receipt } = await client.token.burnSync({
  amount: 100n,
  token: '0x20c0000000000000000000000000000000000001',
})
// @log: { from: '0x…', amount: 100n, receipt: { … } }
```

```ts twoslash [Standalone]
import { Actions } from 'viem/tempo'
import { client } from './viem.config'

const { from, amount, receipt } = await Actions.token.burnSync(client, {
  amount: 100n,
  token: '0x20c0000000000000000000000000000000000001',
})
// @log: { from: '0x…', amount: 100n, receipt: { … } }
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
import { Account } from 'viem'
import { Client } from 'viem/tempo'

export const client = Client.create({
  account: Account.fromPrivateKey('0x…'),
})
```

:::

## Parameters

### amount

- **Type:** `bigint | { decimals?: number, formatted: string }`

Amount of tokens to burn, in base units or formatted decimal form.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const result = await Actions.token.burnSync(client, {
  amount: { decimals: 6, formatted: '1.5' }, // [!code focus]
  token: '0x20c0000000000000000000000000000000000001',
})
```

### feeToken

- **Type:** `TokenId | Address`
- **Default:** `client.feeToken`

TIP-20 token to pay transaction fees with.

### memo

- **Type:** `Hex`

Memo to include in the burn (calls `burnWithMemo`).

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const result = await Actions.token.burnSync(client, {
  amount: 100n,
  memo: '0xdeadbeef', // [!code focus]
  token: '0x20c0000000000000000000000000000000000001',
})
```

### nonceKey

- **Type:** `bigint | 'random'`

Nonce key to send the transaction under. Pass `'random'` to use a random key.

### throwOnReceiptRevert

- **Type:** `boolean`
- **Default:** `true`

Whether to throw when the receipt marks the transaction as reverted.

### token

- **Type:** `TokenId | Address`

Address or ID of the TIP-20 token.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const result = await Actions.token.burnSync(client, {
  amount: 100n,
  token: 1n, // [!code focus]
})
```

## Return Value

`{ from: Address, amount: bigint, receipt: TransactionReceipt }`

The `Burn` event data and the transaction receipt.

## Errors

| Error | Description |
| --- | --- |
| `ContractFunctionExecutionError` | The write failed or the receipt reverted. |
