# channel.requestCloseSync

Starts the payer close timer for a TIP-20 payment channel, and waits for the transaction to be confirmed.

## Usage

:::code-group

```ts twoslash [Decorator]
import { client } from './viem.config'

const { closeGraceEnd, receipt } = await client.channel.requestCloseSync({
  channel: {
    expiringNonceHash: '0x…',
    payee: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
    payer: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
    salt: '0x…',
    token: 1n,
  },
})
// @log: {
// @log:   channelId: '0x…',
// @log:   payer: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
// @log:   payee: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
// @log:   closeGraceEnd: 1717000000n,
// @log:   receipt: { … },
// @log: }
```

```ts twoslash [Standalone]
import { Actions } from 'viem/tempo'
import { client } from './viem.config'

const { closeGraceEnd, receipt } = await Actions.channel.requestCloseSync(
  client,
  {
    channel: {
      expiringNonceHash: '0x…',
      payee: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
      payer: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
      salt: '0x…',
      token: 1n,
    },
  },
)
// @log: {
// @log:   channelId: '0x…',
// @log:   payer: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266',
// @log:   payee: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
// @log:   closeGraceEnd: 1717000000n,
// @log:   receipt: { … },
// @log: }
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
import { Account } from 'viem'
import { Client } from 'viem/tempo'

export const client = Client.create({
  account: Account.fromPrivateKey('0x…'),
})
```

:::

## Parameters

### channel

- **Type:** `Channel`

Channel descriptor: the fields that identify the channel (`payer`, `payee`, `token`, `salt`, `expiringNonceHash`, plus optional `operator` and `authorizedSigner`). Use the event data returned by [`channel.openSync`](/tempo/actions/channel.openSync).

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const result = await Actions.channel.requestCloseSync(client, {
  channel: { // [!code focus]
    expiringNonceHash: '0x…', // [!code focus]
    payee: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8', // [!code focus]
    payer: '0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266', // [!code focus]
    salt: '0x…', // [!code focus]
    token: 1n, // [!code focus]
  }, // [!code focus]
})
```

### feeToken

- **Type:** `TokenId | Address`
- **Default:** `client.feeToken`

TIP-20 token to pay transaction fees with.

### nonceKey

- **Type:** `bigint | 'random'`

Nonce key to send the transaction under. Pass `'random'` to use a random key.

### throwOnReceiptRevert

- **Type:** `boolean`
- **Default:** `true`

Whether to throw when the receipt marks the transaction as reverted.

## Return Value

`{ channelId: Hex, payer: Address, payee: Address, closeGraceEnd: bigint, receipt: TransactionReceipt }`

The `CloseRequested` event data and the transaction receipt. `closeGraceEnd` is the timestamp at which the close grace period ends.

## Errors

| Error | Description |
| --- | --- |
| `ContractFunctionExecutionError` | The write failed or the receipt reverted. |
