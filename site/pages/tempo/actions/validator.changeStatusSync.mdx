# validator.changeStatusSync

Changes a validator's active status, and waits for the transaction to be confirmed.

## Usage

:::code-group

```ts twoslash [Decorator]
import { client } from './viem.config'

const { receipt } = await client.validator.changeStatusSync({
  active: false,
  validator: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
})
// @log: { receipt: { … } }
```

```ts twoslash [Standalone]
import { Actions } from 'viem/tempo'
import { client } from './viem.config'

const { receipt } = await Actions.validator.changeStatusSync(client, {
  active: false,
  validator: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
})
// @log: { receipt: { … } }
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
import { Account } from 'viem'
import { Client } from 'viem/tempo'

export const client = Client.create({
  account: Account.fromPrivateKey('0x…'),
})
```

:::

## Parameters

### active

- **Type:** `boolean`

Whether the validator should be active.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const { receipt } = await Actions.validator.changeStatusSync(client, {
  active: true, // [!code focus]
  validator: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8',
})
```

### feeToken

- **Type:** `TokenId | Address`
- **Default:** `client.feeToken`

TIP-20 token to pay transaction fees with.

### nonceKey

- **Type:** `bigint | 'random'`

Nonce key to send the transaction under. Pass `'random'` to use a random key.

### throwOnReceiptRevert

- **Type:** `boolean`
- **Default:** `true`

Whether to throw when the receipt marks the transaction as reverted.

### validator

- **Type:** `Address`

The validator address.

```ts twoslash
import { Actions } from 'viem/tempo'
import { client } from './viem.config'
// ---cut---
const { receipt } = await Actions.validator.changeStatusSync(client, {
  active: false,
  validator: '0x70997970C51812dc3A010C7d01b50e0d17dc79C8', // [!code focus]
})
```

## Return Value

`{ receipt: TransactionReceipt }`

The transaction receipt.

## Errors

| Error | Description |
| --- | --- |
| `ContractFunctionExecutionError` | The write failed or the receipt reverted. |
