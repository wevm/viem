```ts twoslash
import { Client, fallback, http, webSocket } from 'viem'
import { mainnet } from 'viem/chains'

const client = Client.create({
  chain: mainnet,
  transport: fallback(
    [
      http('https://eth.example.com'),
      http('https://eth-backup.example.com'),
      webSocket('wss://eth.internal:8546'),
    ],
    { rank: true },
  ),
})
```
