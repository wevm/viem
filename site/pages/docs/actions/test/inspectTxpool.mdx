# inspectTxpool

Returns a textual summary of all transactions currently pending for inclusion in the next block(s), as well as the ones being scheduled for future execution.

## Usage

:::code-group

```ts twoslash [Decorator]
import { client } from './viem.config'

const result = await client.inspectTxpool()
// @log: {
// @log:   pending: {
// @log:     '0xa0cf798816d4b9b9866b5330eea46a18382f251e': {
// @log:       '42': '0x...: 1 wei + 21000 gas × 1 wei',
// @log:     },
// @log:   },
// @log:   queued: {},
// @log: }
```

```ts twoslash [Standalone]
import { Actions } from 'viem'
import { client } from './viem.config'

const result = await Actions.test.inspectTxpool(client)
// @log: {
// @log:   pending: {
// @log:     '0xa0cf798816d4b9b9866b5330eea46a18382f251e': {
// @log:       '42': '0x...: 1 wei + 21000 gas × 1 wei',
// @log:     },
// @log:   },
// @log:   queued: {},
// @log: }
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
import { Client, http, testActions } from 'viem'
import { anvil } from 'viem/chains'

export const client = Client.create({
  chain: anvil,
  transport: http(),
}).extend(testActions())
```

:::

## Return Value

`{ pending: Record<Address, Record<string, string>>; queued: Record<Address, Record<string, string>> }`

The pending and queued transactions keyed by sender address.
