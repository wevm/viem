import { Card, Cards } from 'vocs'

# Public Actions

## Overview

**Public Actions** map one-to-one with "public" Ethereum RPC methods (`eth_call`, `eth_getBalance`, `eth_getBlockByNumber`, etc.). They read state and simulate calls against a node, decoding the JSON-RPC response into ergonomic, typed values.

Attach [`publicActions`](#publicactions) to a Client to call them as methods, or call each action standalone by passing a Client as the first argument from the `Actions` namespace:

:::code-group

```ts twoslash [Decorator]
import { client } from './viem.config'

const block = await client.getBlock()
// @log: { number: 19868020n, hash: '0x89644b...', timestamp: 1715852123n, ... }
```

```ts twoslash [Standalone]
import { Actions } from 'viem'
import { client } from './viem.config'

const block = await Actions.getBlock(client)
// @log: { number: 19868020n, hash: '0x89644b...', timestamp: 1715852123n, ... }
```

```ts twoslash [viem.config.ts] filename="viem.config.ts"
import { Client, http, publicActions } from 'viem'
import { mainnet } from 'viem/chains'

export const client = Client.create({
  chain: mainnet,
  transport: http(),
}).extend(publicActions())
```

:::

## Actions

<Cards>
  <Card title="call" description="Execute a message call without submitting a transaction." icon="lucide:phone" to="/docs/actions/public/call" />
  <Card title="getBalance" description="Return the balance of an address in wei." icon="lucide:wallet" to="/docs/actions/public/getBalance" />
  <Card title="getBlobBaseFee" description="Return the base fee per blob gas in wei." icon="lucide:fuel" to="/docs/actions/public/getBlobBaseFee" />
  <Card title="getBlock" description="Return information about a block." icon="lucide:box" to="/docs/actions/public/getBlock" />
  <Card title="getBlockNumber" description="Return the number of the most recent block." icon="lucide:hash" to="/docs/actions/public/getBlockNumber" />
  <Card title="getBlockReceipts" description="Return the transaction receipts of a block." icon="lucide:scroll-text" to="/docs/actions/public/getBlockReceipts" />
  <Card title="getBlockTransactionCount" description="Return the number of transactions in a block." icon="lucide:list" to="/docs/actions/public/getBlockTransactionCount" />
  <Card title="getChainId" description="Return the chain ID of the current network." icon="lucide:link" to="/docs/actions/public/getChainId" />
  <Card title="getCode" description="Retrieve the bytecode at an address." icon="lucide:file-code" to="/docs/actions/public/getCode" />
  <Card title="getGasPrice" description="Return the current price of gas in wei." icon="lucide:fuel" to="/docs/actions/public/getGasPrice" />
  <Card title="getStorageAt" description="Return the value from a contract storage slot." icon="lucide:database" to="/docs/actions/public/getStorageAt" />
  <Card title="getTransaction" description="Return information about a transaction." icon="lucide:receipt" to="/docs/actions/public/getTransaction" />
  <Card title="getTransactionConfirmations" description="Return the number of confirmations for a transaction." icon="lucide:check-check" to="/docs/actions/public/getTransactionConfirmations" />
  <Card title="getTransactionCount" description="Return the number of transactions an account has sent." icon="lucide:list-ordered" to="/docs/actions/public/getTransactionCount" />
  <Card title="getTransactionReceipt" description="Return the receipt of a transaction." icon="lucide:receipt-text" to="/docs/actions/public/getTransactionReceipt" />
</Cards>

## publicActions

Returns a bag of public actions bound to a Client. Pass it to [`.extend()`](/docs/clients/create#extending-a-client).

### Usage

```ts twoslash
import { Client, http, publicActions } from 'viem'
import { mainnet } from 'viem/chains'

const client = Client.create({
  chain: mainnet,
  transport: http(),
}).extend(publicActions())

const block = await client.getBlock()
// @log: { number: 19868020n, hash: '0x89644b...', timestamp: 1715852123n, ... }
```

### Return Value

`(client: Client) => Decorator`

A function that decorates a Client with every public action as a method.
