{
  "name": "site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -w gen:token-lookup && vocs dev",
    "build": "pnpm -w gen:token-lookup && pnpm -w build:types && NODE_OPTIONS='--max-old-space-size=6144' vocs build",
    "twoslash": "vocs twoslash",
    "preview": "vocs preview"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "accounts": "^0.9.0",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-server-dom-webpack": "~19.2.3",
    "tailwindcss": "^4.1.16",
    "viem": "workspace:*",
    "vite": "^8.0.14",
    "vocs": "https://pkg.pr.new/vocs@c13e773",
    "waku": "^1.0.0-beta.6"
  }
}
